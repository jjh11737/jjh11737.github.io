想要挂载，先需要确认想挂载哪个，挂载了没：
```bash
lsblk -f
```
然后获取UUID:
```bash
sudo blkid
```
再创建挂载点，如`/mnt/data1`：
```bash
sudo mkdir /mnt/data1 -p
```
编辑fstab：
```bash
sudo vi /etc/fstab
```
添加一行：
```
UUID=\<YOUR_UUID\> /mnt/data1 ext4(或ntfs) defaults 0 2
```

